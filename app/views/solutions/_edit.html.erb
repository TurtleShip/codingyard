<%= link_to 'Show', @solution %>&nbsp;|&nbsp;
<%= link_to 'See other solutions', yield(:index_path) %>

<h1 class="title"><%= yield(:header_text) %></h1>

<%= render yield(:form_location) %>

<p class="text-center">
  <i><b>Note that <span class="text-danger">the content of the uploaded solution cannot be edited</span>. Only its
    metadata can be edited.</b></i>
</p>

<div id="editor" style="height: 500px"><%= @content %></div>

<script>
    var editor = ace.edit('editor');
    editor.setTheme('ace/theme/twilight');
    editor.getSession().setMode('ace/mode/<%="#{@solution.language.ace_mode}"%>');
    editor.setReadOnly(true);
</script>